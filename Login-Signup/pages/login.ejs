<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login</title>
  </head>
  <body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center px-4">
    <div class="bg-zinc-800 w-full max-w-md p-8 rounded-lg shadow-lg">
      <h3 class="text-3xl font-semibold mb-6 text-center">Login to Your Account</h3>
      <form method="post" action="/login" class="space-y-4">
        <input
          class="w-full px-4 py-2 outline-none bg-zinc-700 rounded-md border border-zinc-600 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 transition"
          type="email"
          placeholder="Email"
          name="email"
          required
        />
        <input
          class="w-full px-4 py-2 outline-none bg-zinc-700 rounded-md border border-zinc-600 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 transition"
          type="password"
          placeholder="Password"
          name="password"
          required
        />
        <input
          class="w-full py-2 bg-blue-500 hover:bg-blue-600 rounded-md font-medium cursor-pointer transition"
          type="submit"
          value="Login"
        />
      </form>

      <div class="text-center mt-4">
        <p class="text-sm">Don't have an account?</p>
        <a
          href="/"
          class="inline-block mt-2 px-4 py-2 text-sm bg-zinc-700 hover:bg-zinc-600 rounded-md transition"
        >
          Go to Signup
        </a>
      </div>
    </div>
  </body>
</html>
